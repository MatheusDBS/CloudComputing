apiVersion: apps/v1
kind: Deployment
metadata:
  name: club-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: club-service
  template:
    metadata:
      labels:
        app: club-service
    spec:
      containers:
        - name: club-service
          image: bookgather/club-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: DB_HOST
              value: mysql-bookgather
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: clubdb
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              value: root
---
apiVersion: v1
kind: Service
metadata:
  name: club-service
spec:
  selector:
    app: club-service
  ports:
    - port: 8080
      targetPort: 8080
